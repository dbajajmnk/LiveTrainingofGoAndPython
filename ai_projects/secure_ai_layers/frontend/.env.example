# Base URL of the FastAPI app (no trailing slash).
VITE_API_BASE_URL=http://127.0.0.1:8010

# Optional: must match backend DEMO_API_KEY when you set that on the server.
# This is NOT your OpenAI secret — OpenAI keys must never appear in the frontend.
# VITE_DEMO_API_KEY=your-demo-shared-secret
